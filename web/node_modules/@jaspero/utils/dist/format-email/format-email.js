"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatEmail = void 0;
/*
 * Optimize an email for storage
 * - Remove whitespace
 * - Lowercase
 * - Remove dots
 */
function formatEmail(email) {
    var formated = email
        .toLowerCase()
        .trim();
    var _a = formated.split('@'), em = _a[0], domain = _a[1];
    return [
        em
            .replace(/\./g, ''),
        domain
    ]
        .join('@');
}
exports.formatEmail = formatEmail;
