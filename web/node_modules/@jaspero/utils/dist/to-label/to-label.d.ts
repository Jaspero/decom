/**
 * Transforms a key from camelCase, snake_case or dash-case
 * in to label format
 *
 * @example
 * toLabel('someLabel')
 *
 * # Result
 * 'Some Label'
 */
export declare function toLabel(key: string): string;
