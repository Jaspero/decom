/**
 * @param items - array
 * @param previousIndex - number
 * @param currentIndex - number
 * @param mutate - mutate original array, or return swapped
 */
export declare function swapItems(items: any[], previousIndex: number, currentIndex: number, mutate?: boolean): void | any[];
